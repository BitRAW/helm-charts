apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ .Values.app.name }}
  name: {{ .Values.app.name }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.app.name }}
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: {{ .Values.app.name }}
    spec:
      containers:
        - name: {{ .Values.app.name }}
          image: {{ .Values.image.name }}
          env: 
            - name: {{ .Values.config.rpcUser }}
              valueFrom:
                configMapKeyRef:
                  key: {{ .Values.config.rpcUser }}
                  name: {{ .Values.config.btcConfigMap }}
            - name: {{ .Values.config.rpcPW }}
              valueFrom:
                secretKeyRef:
                  key: {{ .Values.config.rpcPW }}
                  name: {{ .Values.config.rpcPWSecret }}
          ports:
            - containerPort: {{ .Values.service.port }}
            - containerPort: {{ .Values.service.rpcPort }}
          volumeMounts:
            - mountPath: /bitcoin/.bitcoin
              name: storage
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: {{ .Values.app.name }}-pvc
